{
    "name": "wasm-gif-encoder",
    "version": "1.0.1",
    "description": "wasm-gif-encoder",
    "main": "lib/index.js",
    "scripts": {
        "build": "wasm-pack build --target nodejs --out-dir lib --out-name index && node ./scripts/clear.js",
        "test": "node ./test/test.js"
    },
    "devDependencies": {
        "console-grid": "^2.0.1",
        "eslint": "^8.26.0",
        "eslint-config-plus": "^1.0.5",
        "eslint-plugin-html": "^7.1.0",
        "screencast-gif": "^1.0.1",
        "upng-js": "^2.1.0"
    }
}