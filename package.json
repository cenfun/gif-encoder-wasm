{
    "name": "wasm-gif-encoder",
    "version": "1.0.1",
    "description": "wasm-gif-encoder",
    "main": "index.js",
    "scripts": {
        "build": "wasm-pack build --target nodejs && node ./scripts/build.js",
        "test": "node ./test/test.js"
    },
    "devDependencies": {
        "upng-js": "^2.1.0"
    }
}
