{
    "name": "wasm-gif-encoder",
    "version": "1.0.1",
    "description": "wasm-gif-encoder",
    "main": "lib/index.js",
    "scripts": {
        "build": "wasm-pack build --target nodejs --out-dir lib --out-name index && node ./scripts/build.js",
        "test": "node ./test/test.js"
    },
    "devDependencies": {
        "screencast-gif": "^1.0.1",
        "upng-js": "^2.1.0"
    }
}
